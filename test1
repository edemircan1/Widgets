/** @jsx jsx */
import { React, jsx } from "jimu-core";

const MVT_STYLE_URL = "url//api/vector/styles/all/style.json?coverageLayer=rsrp_dbm";
const MANUAL_BEARER = "token";
const ARCGIS_API_VERSION = "4.32";

interface State {
  message: string;
  currentZoom: number;
}

export default class Widget extends React.PureComponent<any, State> {
  private mapDiv: HTMLDivElement;
  private mapView: any;
  private originalFetch = window.fetch;

  constructor(props) {
    super(props);
    this.state = {
      message: "Widget yükleniyor...",
      currentZoom: 6
    };
  }

  componentDidMount() {
    this.loadArcGISAPI();
  }

  componentWillUnmount() {
    if (this.mapView) {
      this.mapView.destroy();
    }
    window.fetch = this.originalFetch;
  }

  loadArcGISAPI = () => {
    this.setState({ message: "📦 ArcGIS API yükleniyor..." });

    if ((window as any).require) {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
      return;
    }

    const cssLink = document.createElement("link");
    cssLink.rel = "stylesheet";
    cssLink.href = `https://js.arcgis.com/${ARCGIS_API_VERSION}/esri/themes/dark/main.css`;
    document.head.appendChild(cssLink);

    const existing = document.querySelector(`script[data-arcgis-api="${ARCGIS_API_VERSION}"]`) as HTMLScriptElement | null;
    if (existing) {
      existing.addEventListener('load', () => {
        this.setState({ message: "✅ ArcGIS API yüklendi" });
        this.createMap();
      });
      return;
    }

    const script = document.createElement("script");
    script.src = `https://js.arcgis.com/${ARCGIS_API_VERSION}/`;
    script.setAttribute('data-arcgis-api', ARCGIS_API_VERSION);
    script.onload = () => {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
    };
    script.onerror = () => {
      this.setState({ message: "❌ ArcGIS API yüklenemedi" });
    };
    document.head.appendChild(script);
  };

  createMap = () => {
    this.setState({ message: "🗺️ Map oluşturuluyor..." });

    if (!this.mapDiv) {
      setTimeout(() => this.createMap(), 50);
      return;
    }

    if (!(window as any).require) {
      this.setState({ message: "❌ ArcGIS API loader bulunamadı" });
      return;
    }

    (window as any).require([
      'esri/Map',
      'esri/views/MapView',
      'esri/layers/VectorTileLayer',
      'esri/request'
    ], (Map, MapView, VectorTileLayer, esriRequest) => {
      
      const map = new Map({
        basemap: 'dark-gray-vector'
      });

      this.mapView = new MapView({
        container: this.mapDiv,
        map: map,
        center: [32, 39],
        zoom: 6,
        constraints: {
          snapToZoom: false,  // ✅ Smooth zoom
          minZoom: 3,
          maxZoom: 20
        }
      });

      // Zoom tracking
      this.mapView.watch('zoom', (newZoom) => {
        this.setState({ currentZoom: Math.round(newZoom * 10) / 10 });
      });

      this.setState({ message: "✅ Map oluşturuldu! MVT yükleniyor..." });

      // esriRequest ile Authorization header ekle
      esriRequest(MVT_STYLE_URL, {
        headers: {
          'Authorization': `Bearer ${MANUAL_BEARER}`
        },
        responseType: 'json'
      }).then(response => {
        this.setState({ message: "📄 Style JSON alındı" });
        let styleJson = response.data;
        
        console.log("📥 Original Style JSON:", JSON.stringify(styleJson, null, 2));
        
        // ✅ TILE URL'LERİNİ KONTROL ET
        Object.keys(styleJson.sources).forEach(sourceKey => {
          const source = styleJson.sources[sourceKey];
          console.log(`🔍 Source: ${sourceKey}`);
          console.log(`   Tiles:`, source.tiles);
          console.log(`   Min/Max Zoom:`, source.minzoom, '/', source.maxzoom);
        });

        // ✅ OPTİMİZASYON 1: Line layer'ları kaldır (siyah çizgileri önler)
        styleJson = {
          ...styleJson,
          layers: styleJson.layers.filter(layer => layer.type === 'fill')
        };

        // ✅ OPTİMİZASYON 2: Fill opacity artır (daha belirgin)
        styleJson.layers = styleJson.layers.map(layer => ({
          ...layer,
          paint: {
            ...layer.paint,
            'fill-opacity': 0.7,  // 0.5'ten 0.7'ye çıkardık
            'fill-antialias': true
          }
        }));

        console.log("📤 Modified Style JSON:", styleJson);

        // VectorTileLayer oluştur
        const mvtLayer = new VectorTileLayer({
          style: styleJson,
          opacity: 1
        });

        // ✅ Layer load olunca fetch override yap
        mvtLayer.when(() => {
          this.setState({ message: "✅ MVT Layer yüklendi!" });
          
          // Override fetch for tile requests - ORİJİNAL YÖNTEM
          const originalFetch = window.fetch;
          window.fetch = (url, options = {}) => {
            const urlStr = url.toString();
            if (urlStr.includes(new URL(MVT_STYLE_URL).hostname) || urlStr.includes('/api/vector/tile')) {
              console.log("🔐 Tile request intercepted:", urlStr);
              console.log("📋 Request options:", options);
              console.log("🔑 Authorization header:", options['headers']?.['Authorization'] || 'MISSING!');
              return originalFetch(url, {
                ...options,
                headers: {
                  ...(options['headers'] || {}),
                  'Authorization': `Bearer ${MANUAL_BEARER}`
                }
              }).then(resp => {
                console.log(`📡 Response for ${urlStr}:`, resp.status, resp.statusText);
                return resp;
              }).catch(err => {
                console.error(`❌ Fetch error for ${urlStr}:`, err);
                throw err;
              });
            }
            return originalFetch(url, options);
          };
        }).catch(err => {
          this.setState({ message: `❌ Layer error: ${err.message}` });
          console.error("❌ Layer hatası:", err);
        });

        map.add(mvtLayer);
        
      }).catch(err => {
        this.setState({ message: `❌ Fetch error: ${err.message}` });
        console.error("❌ FETCH HATASI detay:", err);
        console.error("URL:", MVT_STYLE_URL);
        console.error("Token:", MANUAL_BEARER ? "Var" : "YOK!");
      });
    }, (err) => {
      this.setState({ message: `❌ Loader error: ${err?.message || err}` });
      console.error('❌ AMD require error:', err);
    });
  };

  render() {
    const { message, currentZoom } = this.state;

    return (
      <div style={{ width: "100%", height: "100%", position: "relative" }}>
        <div 
          ref={el => this.mapDiv = el}
          style={{ width: "100%", height: "100%" }}
        />

        <div style={{
          position: "absolute",
          top: "10px",
          left: "10px",
          background: "rgba(0,0,0,0.85)",
          color: "white",
          padding: "15px",
          borderRadius: "8px",
          zIndex: 9999,
          fontFamily: "monospace",
          fontSize: "12px",
          minWidth: "280px",
          boxShadow: "0 4px 6px rgba(0,0,0,0.3)"
        }}>
          <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
            <strong style={{ color: "#4CAF50" }}>📡 MVT Coverage</strong>
            <span style={{ 
              fontSize: "10px", 
              background: "#2196F3", 
              padding: "2px 6px", 
              borderRadius: "3px" 
            }}>
              Optimized
            </span>
          </div>
          
          <div style={{ 
            marginTop: "10px", 
            borderTop: "1px solid rgba(255,255,255,0.2)", 
            paddingTop: "10px" 
          }}>
            {message}
          </div>
          
          <div style={{ 
            marginTop: "10px",
            padding: "8px",
            background: "rgba(255,255,255,0.05)",
            borderRadius: "4px",
            fontSize: "11px"
          }}>
            <div style={{ marginBottom: "4px" }}>
              Zoom: <span style={{ color: "#FFD700", fontWeight: "bold" }}>{currentZoom}</span>
            </div>
            <div style={{ fontSize: "10px", color: "#888" }}>
              Active: {
                currentZoom < 7 ? "3z tiles (zoom 3-7)" :
                currentZoom < 11 ? "7z tiles (zoom 7-11)" :
                currentZoom < 15 ? "11z tiles (zoom 11-15)" : "15z tiles (zoom 15+)"
              }
            </div>
          </div>

          <div style={{
            marginTop: "8px",
            fontSize: "9px",
            color: "#666",
            borderTop: "1px solid rgba(255,255,255,0.1)",
            paddingTop: "6px"
          }}>
            ✨ No line borders • Higher opacity • Smooth zoom
          </div>
        </div>
      </div>
    );
  }
}
