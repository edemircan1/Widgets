/** @jsx jsx */
import { React, jsx } from "jimu-core";

const MVT_STYLE_URL = "/styles/all/style.json?coverageLayer=rsrp_dbm";
const MANUAL_BEARER = "token";
const ARCGIS_API_VERSION = "4.32";

interface State {
  message: string;
  currentZoom: number;
  testMode: string;
}

export default class Widget extends React.PureComponent<any, State> {
  private mapDiv: HTMLDivElement;
  private mapView: any;
  private map: any;
  private currentLayers: any[] = [];

  constructor(props) {
    super(props);
    this.state = {
      message: "Widget yükleniyor...",
      currentZoom: 15,
      testMode: "none"
    };
  }

  componentDidMount() {
    this.loadArcGISAPI();
  }

  componentWillUnmount() {
    if (this.mapView) {
      this.mapView.destroy();
    }
  }

  loadArcGISAPI = () => {
    this.setState({ message: "📦 ArcGIS API yükleniyor..." });

    if ((window as any).require) {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
      return;
    }

    const cssLink = document.createElement("link");
    cssLink.rel = "stylesheet";
    cssLink.href = `https://js.arcgis.com/${ARCGIS_API_VERSION}/esri/themes/dark/main.css`;
    document.head.appendChild(cssLink);

    const script = document.createElement("script");
    script.src = `https://js.arcgis.com/${ARCGIS_API_VERSION}/`;
    script.setAttribute('data-arcgis-api', ARCGIS_API_VERSION);
    script.onload = () => {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
    };
    script.onerror = () => {
      this.setState({ message: "❌ ArcGIS API yüklenemedi" });
    };
    document.head.appendChild(script);
  };

  createMap = () => {
    this.setState({ message: "🗺️ Map oluşturuluyor..." });

    if (!this.mapDiv) {
      setTimeout(() => this.createMap(), 50);
      return;
    }

    if (!(window as any).require) {
      this.setState({ message: "❌ ArcGIS API loader bulunamadı" });
      return;
    }

    (window as any).require([
      'esri/Map',
      'esri/views/MapView'
    ], (Map, MapView) => {
      
      this.map = new Map({
        basemap: 'dark-gray-vector'
      });

      this.mapView = new MapView({
        container: this.mapDiv,
        map: this.map,
        center: [32, 39],
        zoom: 15,
        constraints: {
          snapToZoom: false,
          minZoom: 3,
          maxZoom: 22,
          rotationEnabled: false
        }
      });

      this.mapView.watch('zoom', (newZoom) => {
        this.setState({ currentZoom: Math.round(newZoom * 10) / 10 });
      });

      this.setState({ message: "✅ Map hazır! Test seçin..." });

      // Setup fetch override for authorization
      const originalFetch = window.fetch;
      window.fetch = (url, options = {}) => {
        const urlStr = url.toString();
        if (urlStr.includes('oauth21') || urlStr.includes('vector/tile')) {
          return originalFetch(url, {
            ...options,
            headers: {
              ...(options['headers'] || {}),
              'Authorization': `Bearer ${MANUAL_BEARER}`
            }
          });
        }
        return originalFetch(url, options);
      };
      
    }, (err) => {
      this.setState({ message: `❌ Loader error: ${err?.message || err}` });
    });
  };

  clearLayers = () => {
    this.currentLayers.forEach(layer => {
      this.map.remove(layer);
    });
    this.currentLayers = [];
  };

  runTest = (mode: string) => {
    this.setState({ testMode: mode, message: `🧪 Test: ${mode}` });
    this.clearLayers();

    (window as any).require([
      'esri/layers/VectorTileLayer',
      'esri/request'
    ], (VectorTileLayer, esriRequest) => {

      if (mode === 'test1_no_filter') {
        this.test1_NoFilter(VectorTileLayer, esriRequest);
      } else if (mode === 'test2_with_filter') {
        this.test2_WithFilter(VectorTileLayer, esriRequest);
      } else if (mode === 'test3_simple_filter') {
        this.test3_SimpleFilter(VectorTileLayer, esriRequest);
      }
    });
  };

  // TEST 1: Filter OLMADAN, sadece static renk
  test1_NoFilter = (VectorTileLayer, esriRequest) => {
    console.log("🧪 TEST 1: No filter, just static red color");
    
    esriRequest(MVT_STYLE_URL, {
      headers: { 'Authorization': `Bearer ${MANUAL_BEARER}` },
      responseType: 'json'
    }).then(response => {
      const originalStyle = response.data;

      const customStyle = {
        version: 8,
        sources: {
          'coverage_15z': originalStyle.sources['coverage_15z']
        },
        layers: [
          {
            id: 'fill_test',
            type: 'fill',
            source: 'coverage_15z',
            'source-layer': 'data',
            minzoom: 15,
            maxzoom: 24,
            paint: {
              'fill-color': '#FF0000',
              'fill-opacity': 0.7
            }
          }
        ]
      };

      console.log("Style (no filter):", customStyle);

      const layer = new VectorTileLayer({
        style: customStyle,
        minScale: 0,
        maxScale: 0,
        title: 'Test1_NoFilter'
      });

      layer.when(() => {
        console.log("✅ Layer loaded (no filter)");
        this.setState({ message: "✅ Test 1: Layer yüklendi (kırmızı görünmeli)" });
      }).catch(err => {
        console.error("❌ Layer error:", err);
        this.setState({ message: `❌ Test 1 failed: ${err.message}` });
      });

      this.map.add(layer);
      this.currentLayers.push(layer);

    }).catch(err => {
      this.setState({ message: `❌ Style fetch failed: ${err.message}` });
    });
  };

  // TEST 2: Filter ile (>=, <)
  test2_WithFilter = (VectorTileLayer, esriRequest) => {
    console.log("🧪 TEST 2: With filter (RSRP >= -100)");
    
    esriRequest(MVT_STYLE_URL, {
      headers: { 'Authorization': `Bearer ${MANUAL_BEARER}` },
      responseType: 'json'
    }).then(response => {
      const originalStyle = response.data;

      const customStyle = {
        version: 8,
        sources: {
          'coverage_15z': originalStyle.sources['coverage_15z']
        },
        layers: [
          {
            id: 'fill_test',
            type: 'fill',
            source: 'coverage_15z',
            'source-layer': 'data',
            minzoom: 15,
            maxzoom: 24,
            filter: ['>=', ['get', 'rsrp_dbm'], -100],
            paint: {
              'fill-color': '#00FF00',
              'fill-opacity': 0.7
            }
          }
        ]
      };

      console.log("Style (with filter >= -100):", customStyle);

      const layer = new VectorTileLayer({
        style: customStyle,
        minScale: 0,
        maxScale: 0,
        title: 'Test2_WithFilter'
      });

      layer.when(() => {
        console.log("✅ Layer loaded (with filter)");
        this.setState({ message: "✅ Test 2: Layer yüklendi (yeşil görünmeli, sadece RSRP>-100)" });
      }).catch(err => {
        console.error("❌ Layer error:", err);
        this.setState({ message: `❌ Test 2 failed: ${err.message}` });
      });

      this.map.add(layer);
      this.currentLayers.push(layer);

    }).catch(err => {
      this.setState({ message: `❌ Style fetch failed: ${err.message}` });
    });
  };

  // TEST 3: Simple filter (has property)
  test3_SimpleFilter = (VectorTileLayer, esriRequest) => {
    console.log("🧪 TEST 3: Simple filter (has rsrp_dbm)");
    
    esriRequest(MVT_STYLE_URL, {
      headers: { 'Authorization': `Bearer ${MANUAL_BEARER}` },
      responseType: 'json'
    }).then(response => {
      const originalStyle = response.data;

      const customStyle = {
        version: 8,
        sources: {
          'coverage_15z': originalStyle.sources['coverage_15z']
        },
        layers: [
          {
            id: 'fill_test',
            type: 'fill',
            source: 'coverage_15z',
            'source-layer': 'data',
            minzoom: 15,
            maxzoom: 24,
            filter: ['has', 'rsrp_dbm'],
            paint: {
              'fill-color': '#0000FF',
              'fill-opacity': 0.7
            }
          }
        ]
      };

      console.log("Style (simple filter - has property):", customStyle);

      const layer = new VectorTileLayer({
        style: customStyle,
        minScale: 0,
        maxScale: 0,
        title: 'Test3_SimpleFilter'
      });

      layer.when(() => {
        console.log("✅ Layer loaded (simple filter)");
        this.setState({ message: "✅ Test 3: Layer yüklendi (mavi görünmeli)" });
      }).catch(err => {
        console.error("❌ Layer error:", err);
        this.setState({ message: `❌ Test 3 failed: ${err.message}` });
      });

      this.map.add(layer);
      this.currentLayers.push(layer);

    }).catch(err => {
      this.setState({ message: `❌ Style fetch failed: ${err.message}` });
    });
  };

  render() {
    const { message, currentZoom, testMode } = this.state;

    return (
      <div style={{ width: "100%", height: "100%", position: "relative" }}>
        <div 
          ref={el => this.mapDiv = el}
          style={{ width: "100%", height: "100%" }}
        />

        {/* Test Panel */}
        <div style={{
          position: "absolute",
          top: "10px",
          right: "10px",
          background: "rgba(0,0,0,0.95)",
          color: "white",
          padding: "15px",
          borderRadius: "8px",
          zIndex: 9999,
          fontFamily: "monospace",
          fontSize: "11px",
          maxWidth: "280px"
        }}>
          <div style={{ marginBottom: "12px", fontWeight: "bold", color: "#4CAF50" }}>
            🧪 FILTER TESTS
          </div>
          
          <div style={{ marginBottom: "10px", padding: "8px", background: "rgba(255,255,255,0.1)", borderRadius: "4px", fontSize: "10px" }}>
            <div>Zoom: {currentZoom}</div>
            <div>Mode: {testMode || "none"}</div>
          </div>

          <button onClick={() => this.runTest('test1_no_filter')} style={buttonStyle}>
            Test 1: No Filter (RED)
          </button>

          <button onClick={() => this.runTest('test2_with_filter')} style={buttonStyle}>
            Test 2: Filter >= -100 (GREEN)
          </button>

          <button onClick={() => this.runTest('test3_simple_filter')} style={buttonStyle}>
            Test 3: Has Property (BLUE)
          </button>

          <button 
            onClick={() => {
              this.clearLayers();
              this.setState({ message: "✅ Temizlendi", testMode: "none" });
            }} 
            style={{...buttonStyle, background: "#f44336", marginTop: "10px"}}
          >
            🗑️ Clear
          </button>

          <div style={{ fontSize: "9px", color: "#aaa", marginTop: "10px", lineHeight: "1.3" }}>
            💡 Test 1: Filter yok → MVT görünmeli<br/>
            💡 Test 2: Comparison filter → Kısmi MVT<br/>
            💡 Test 3: Simple filter → MVT görünmeli
          </div>
        </div>

        {/* Status */}
        <div style={{
          position: "absolute",
          bottom: "10px",
          left: "10px",
          background: "rgba(0,0,0,0.9)",
          color: "white",
          padding: "12px",
          borderRadius: "5px",
          zIndex: 9999,
          fontFamily: "monospace",
          fontSize: "10px",
          maxWidth: "350px"
        }}>
          <div style={{ color: "#4CAF50" }}>{message}</div>
        </div>
      </div>
    );
  }
}

const buttonStyle = {
  width: "100%",
  padding: "10px 8px",
  marginBottom: "5px",
  background: "#2196F3",
  color: "white",
  border: "none",
  borderRadius: "4px",
  cursor: "pointer",
  fontSize: "11px",
  textAlign: "left" as const,
  fontFamily: "monospace"
};
