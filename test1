/** @jsx jsx */
import { React, jsx } from "jimu-core";

const MVT_STYLE_URL = "/styles/all/style.json?coverageLayer=rsrp_dbm";
const MANUAL_BEARER = "token";
const ARCGIS_API_VERSION = "4.32";
const MAPLIBRE_VERSION = "3.6.2";

interface State {
  message: string;
  currentZoom: number;
}

export default class Widget extends React.PureComponent<any, State> {
  private mapDiv: HTMLDivElement;
  private maplibreDiv: HTMLDivElement;
  private mapView: any;
  private maplibreMap: any;

  constructor(props) {
    super(props);
    this.state = {
      message: "Widget yükleniyor...",
      currentZoom: 15
    };
  }

  componentDidMount() {
    this.loadMapLibre();
    this.loadArcGISAPI();
  }

  componentWillUnmount() {
    if (this.mapView) {
      this.mapView.destroy();
    }
    if (this.maplibreMap) {
      this.maplibreMap.remove();
    }
  }

  loadMapLibre = () => {
    // MapLibre CSS
    const cssLink = document.createElement("link");
    cssLink.rel = "stylesheet";
    cssLink.href = `https://unpkg.com/maplibre-gl@${MAPLIBRE_VERSION}/dist/maplibre-gl.css`;
    document.head.appendChild(cssLink);

    // MapLibre JS
    const script = document.createElement("script");
    script.src = `https://unpkg.com/maplibre-gl@${MAPLIBRE_VERSION}/dist/maplibre-gl.js`;
    script.onload = () => {
      console.log("✅ MapLibre GL JS loaded");
    };
    document.head.appendChild(script);
  };

  loadArcGISAPI = () => {
    this.setState({ message: "📦 ArcGIS API yükleniyor..." });

    if ((window as any).require) {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
      return;
    }

    const cssLink = document.createElement("link");
    cssLink.rel = "stylesheet";
    cssLink.href = `https://js.arcgis.com/${ARCGIS_API_VERSION}/esri/themes/dark/main.css`;
    document.head.appendChild(cssLink);

    const existing = document.querySelector(`script[data-arcgis-api="${ARCGIS_API_VERSION}"]`) as HTMLScriptElement | null;
    if (existing) {
      existing.addEventListener('load', () => {
        this.setState({ message: "✅ ArcGIS API yüklendi" });
        this.createMap();
      });
      return;
    }

    const script = document.createElement("script");
    script.src = `https://js.arcgis.com/${ARCGIS_API_VERSION}/`;
    script.setAttribute('data-arcgis-api', ARCGIS_API_VERSION);
    script.onload = () => {
      this.setState({ message: "✅ ArcGIS API yüklendi" });
      this.createMap();
    };
    script.onerror = () => {
      this.setState({ message: "❌ ArcGIS API yüklenemedi" });
    };
    document.head.appendChild(script);
  };

  createMap = () => {
    this.setState({ message: "🗺️ Map oluşturuluyor..." });

    if (!this.mapDiv || !this.maplibreDiv) {
      setTimeout(() => this.createMap(), 50);
      return;
    }

    if (!(window as any).require) {
      this.setState({ message: "❌ ArcGIS API loader bulunamadı" });
      return;
    }

    (window as any).require([
      'esri/Map',
      'esri/views/MapView'
    ], (Map, MapView) => {
      
      const map = new Map({
        basemap: 'dark-gray-vector'
      });

      this.mapView = new MapView({
        container: this.mapDiv,
        map: map,
        center: [32, 39],
        zoom: 15,
        constraints: {
          snapToZoom: false,
          minZoom: 3,
          maxZoom: 22,
          rotationEnabled: false
        }
      });

      this.mapView.when(() => {
        this.setState({ message: "✅ ArcGIS Map hazır, MapLibre yükleniyor..." });
        this.createMapLibreOverlay();
        
        // Sync MapLibre with ArcGIS
        this.mapView.watch(['center', 'zoom', 'rotation'], () => {
          if (this.maplibreMap && !this.maplibreMap.isMoving()) {
            const center = this.mapView.center;
            this.maplibreMap.setCenter([center.longitude, center.latitude]);
            this.maplibreMap.setZoom(this.mapView.zoom);
            this.maplibreMap.setBearing(-this.mapView.rotation);
          }
        });

        this.mapView.watch('zoom', (newZoom) => {
          this.setState({ currentZoom: Math.round(newZoom * 10) / 10 });
        });
      });
    });
  };

  createMapLibreOverlay = () => {
    if (!(window as any).maplibregl) {
      setTimeout(() => this.createMapLibreOverlay(), 100);
      return;
    }

    this.setState({ message: "🎨 MapLibre overlay oluşturuluyor..." });

    // Fetch override for authorization
    const originalFetch = window.fetch;
    window.fetch = (url, options = {}) => {
      const urlStr = url.toString();
      if (urlStr.includes('abc.com')) {
        console.log(`🔍 MapLibre tile request: ${urlStr}`);
        return originalFetch(url, {
          ...options,
          headers: {
            ...(options['headers'] || {}),
            'Authorization': `Bearer ${MANUAL_BEARER}`
          }
        });
      }
      return originalFetch(url, options);
    };

    // Fetch style JSON
    fetch(MVT_STYLE_URL, {
      headers: {
        'Authorization': `Bearer ${MANUAL_BEARER}`
      }
    })
    .then(res => res.json())
    .then(styleJson => {
      console.log("📋 Style JSON received:", styleJson);

      const maplibregl = (window as any).maplibregl;
      
      // MapLibre harita oluştur (transparent basemap)
      this.maplibreMap = new maplibregl.Map({
        container: this.maplibreDiv,
        style: {
          version: 8,
          sources: styleJson.sources,
          layers: styleJson.layers,
          glyphs: "https://demotiles.maplibre.org/font/{fontstack}/{range}.pbf"
        },
        center: [32, 39],
        zoom: 15,
        interactive: false, // ArcGIS handle etsin
        attributionControl: false
      });

      this.maplibreMap.on('load', () => {
        console.log("✅ MapLibre map loaded with colored MVT!");
        this.setState({ message: "✅ Renkli MVT layer yüklendi!" });

        // Debug: Layer'ları listele
        const layers = this.maplibreMap.getStyle().layers;
        console.log("📊 MapLibre layers:", layers.map(l => l.id));
      });

      this.maplibreMap.on('error', (e) => {
        console.error("❌ MapLibre error:", e);
      });

      // MapLibre'ı ArcGIS ile sync et
      this.mapView.watch(['center', 'zoom'], () => {
        const center = this.mapView.center;
        this.maplibreMap.setCenter([center.longitude, center.latitude]);
        this.maplibreMap.setZoom(this.mapView.zoom);
      });

    })
    .catch(err => {
      console.error("❌ Style fetch error:", err);
      this.setState({ message: `❌ Style fetch error: ${err.message}` });
    });
  };

  render() {
    const { message, currentZoom } = this.state;

    return (
      <div style={{ width: "100%", height: "100%", position: "relative" }}>
        {/* ArcGIS Basemap (alttta) */}
        <div 
          ref={el => this.mapDiv = el}
          style={{ width: "100%", height: "100%", position: "absolute", top: 0, left: 0 }}
        />

        {/* MapLibre Overlay (üstte, pointer-events: none) */}
        <div 
          ref={el => this.maplibreDiv = el}
          style={{ 
            width: "100%", 
            height: "100%", 
            position: "absolute", 
            top: 0, 
            left: 0,
            pointerEvents: "none", // ArcGIS map'e tıklamaları geçir
            zIndex: 1
          }}
        />

        {/* Zoom Control Panel */}
        <div style={{
          position: "absolute",
          top: "10px",
          right: "10px",
          background: "rgba(0,0,0,0.9)",
          color: "white",
          padding: "20px",
          borderRadius: "8px",
          zIndex: 9999,
          fontFamily: "monospace",
          fontSize: "14px",
          minWidth: "200px"
        }}>
          <div style={{ marginBottom: "15px", textAlign: "center", fontWeight: "bold", color: "#4CAF50" }}>
            🎮 HYBRID MODE
          </div>
          
          <div style={{ 
            background: "rgba(255,255,255,0.1)", 
            padding: "15px", 
            borderRadius: "5px",
            marginBottom: "15px",
            textAlign: "center"
          }}>
            <div style={{ fontSize: "12px", color: "#aaa", marginBottom: "5px" }}>
              Zoom Level
            </div>
            <div style={{ fontSize: "32px", fontWeight: "bold", color: "#FFD700" }}>
              {currentZoom}
            </div>
          </div>

          <div style={{ display: "flex", gap: "10px", marginBottom: "15px" }}>
            <button
              onClick={() => {
                if (this.mapView) {
                  const newZoom = this.mapView.zoom - 0.1;
                  this.mapView.goTo({ zoom: newZoom }, { duration: 200 });
                }
              }}
              style={{
                flex: 1,
                padding: "15px",
                fontSize: "20px",
                background: "#f44336",
                color: "white",
                border: "none",
                borderRadius: "5px",
                cursor: "pointer",
                fontWeight: "bold"
              }}
            >
              -
            </button>
            <button
              onClick={() => {
                if (this.mapView) {
                  const newZoom = this.mapView.zoom + 0.1;
                  this.mapView.goTo({ zoom: newZoom }, { duration: 200 });
                }
              }}
              style={{
                flex: 1,
                padding: "15px",
                fontSize: "20px",
                background: "#4CAF50",
                color: "white",
                border: "none",
                borderRadius: "5px",
                cursor: "pointer",
                fontWeight: "bold"
              }}
            >
              +
            </button>
          </div>

          <div style={{ fontSize: "10px", color: "#FFD700", textAlign: "center" }}>
            🎨 MapLibre + ArcGIS
          </div>
        </div>

        {/* Status Panel */}
        <div style={{
          position: "absolute",
          top: "10px",
          left: "10px",
          background: "rgba(0,0,0,0.8)",
          color: "white",
          padding: "15px",
          borderRadius: "5px",
          zIndex: 9999,
          fontFamily: "monospace",
          fontSize: "12px"
        }}>
          <strong>🚀 Hybrid Render:</strong>
          <div style={{ marginTop: "10px" }}>{message}</div>
          <div style={{ marginTop: "5px", fontSize: "11px", color: "#4CAF50" }}>
            Grid: {
              currentZoom < 7 ? "10km (3z)" :
              currentZoom < 11 ? "1km (7z)" :
              currentZoom < 15 ? "100m (11z)" : "10m (15z)"
            }
          </div>
          <div style={{ marginTop: "10px", fontSize: "10px", color: "#888" }}>
            Basemap: ArcGIS<br/>
            MVT: MapLibre GL
          </div>
        </div>
      </div>
    );
  }
}
